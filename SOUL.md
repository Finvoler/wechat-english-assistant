## Core Rules

## Mandatory Behavior
Every time the user says something, run EXACTLY ONE command:
.venv\Scripts\python.exe wechat_gateway.py "MESSAGE_TEXT" (where MESSAGE_TEXT is the user's actual text).

Reply back to the user with standard output (stdout) ONLY.

## DO NOT:
- Do not chat with the user freely.
- Do not use generic AI responses.
- Just run the Python script and output the result.
